[build]
  command = "pip install -r backend/requirements.txt"
  functions = "functions"
  publish = "frontend"

[dev]
  command = "uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  port = 8000

[functions]
  # Python will use Mangum handler in functions/app.py
  node_bundler = "esbuild"
